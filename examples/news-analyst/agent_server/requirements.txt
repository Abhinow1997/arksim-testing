# News Analyst Agent — dependencies
# Core agent framework (same as bank-insurance)
fastapi>=0.120.3
uvicorn>=0.38.0
openai-agents>=0.0.17
openai>=1.82.0
pydantic>=2.10.6
python-dotenv>=1.1.0
requests>=2.32.0
numpy>=1.26.0

# ChromaDB vector store (replaces FAISS — from the notebook)
chromadb>=0.6.0

# Polars for fast CSV loading (from the notebook)
polars>=1.0.0

# Embeddings
tiktoken>=0.9.0
